# Daily Log — 2026-07-09

## Summary
Sprint 1 continued: completed Next.js frontend pages and committed web scaffold to `main`.

## Completed Tasks
- Created missing dashboard pages:
  - `/projects` — project list + create form
  - `/workflows` — workflow list for selected project
  - `/runs` — placeholder execution history page
- Added `apps/web/.env.example` with `NEXT_PUBLIC_API_URL`
- Verified `pnpm install` works for `apps/web` (10 workspace projects, 189 added)
- Updated `ROADMAP.md` to mark v0.2 Basic Control Room items complete
- Updated `TODO.md` to reflect current completion status

## Files Changed
- `apps/web/src/app/(dashboard)/projects/page.tsx` (new)
- `apps/web/src/app/(dashboard)/workflows/page.tsx` (new)
- `apps/web/src/app/(dashboard)/runs/page.tsx` (new)
- `apps/web/.env.example` (new)
- `ROADMAP.md` (updated)
- `TODO.md` (updated)

## Architecture Decisions
- **Route groups**: continue using `(auth)` and `(dashboard)` for shared layouts without URL segments
- **Workflow filtering**: workflows page reads `projectId` from query string; parent project links pass `?projectId=...`
- **Runs page**: placeholder until executions endpoints are finalized in API

## Known Issues
- `pnpm-lock.yaml` has large uncommitted changes from workspace install; needs commit alongside web app
- Local Next.js build/typecheck not yet validated; rely on CI
- Router paths still expose `(dashboard)` segment in some internal refs; no user-facing impact

## Next Planned Tasks
- Add `.env.example` at root for Docker Compose variables
- Validate `docker compose up` end-to-end against local API
- Start React Flow workflow builder
- Add execution history API + frontend page
- Implement `apps/agent-runner` polling + execution engine

## Estimated Progress %
- v0.1 Core Runtime: ~90%
- v0.2 Basic Control Room: ~45%
- Overall: ~38%
