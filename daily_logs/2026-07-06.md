# Daily Log — 2026-07-06

## Summary
Sprint 1 started. Implemented real JWT auth in `apps/api` backed by Postgres + Prisma + bcrypt.

## Completed Tasks
- Replaced stub `AuthService` with real implementation using PrismaService
- `validateUser` now queries `User` by email and verifies bcrypt password hash
- Added `register` endpoint for new user creation with hashed passwords
- `login` returns JWT with user claims including `name`
- Updated `AuthModule` to import `AppConfigModule` for Prisma dependency
- Configured JWT module with env secret and 1d expiration
- Added `auth.service.spec.ts` unit test scaffold
- Updated `ROADMAP.md` to mark v0.1 Core Runtime items complete

## Files Changed
- `apps/api/src/auth/auth.service.ts`
- `apps/api/src/auth/auth.controller.ts`
- `apps/api/src/auth/auth.module.ts`
- `apps/api/src/auth/auth.service.spec.ts`
- `ROADMAP.md`

## Architecture Decisions
- **Register endpoint**: Added to `AuthController` at `POST /api/v1/auth/register` for self-service signup
- **JWT expiration**: Set to `1d` for session length; configurable via `JWT_SECRET` env
- **Password hashing**: bcrypt with cost factor 10
- **Auth dependency**: `AuthModule` imports `AppConfigModule` to access `PrismaService`

## Known Issues
- Local `tsc --noEmit` times out in this shell environment; relies on CI for typecheck
- `@nestjs/swagger` not installed (declared but install hit timeout earlier)
- Next.js frontend not yet scaffolded

## Next Planned Tasks
- Scaffold Next.js frontend (`apps/web`) with TypeScript + Tailwind
- Add login/register UI
- Dashboard: project list, workflow list
- Validate `docker compose up` end-to-end

## Estimated Progress %
- v0.1 Core Runtime: ~85% (auth real, endpoints done, needs E2E runner test)
- Overall: ~30%

## Token-saving notes
- Skipped local TypeScript validation due to shell timeout; CI will catch errors
- Kept auth implementation minimal to avoid bloat
